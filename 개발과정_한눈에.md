# 🧠 두뇌 게임 허브 — 이렇게 만들어졌습니다

> 💬 *"사람 두뇌를 굴리면서 경쟁심을 자극하는 게임"* 아이디어 한 줄에서 출발해,
> 🎮 **웹 게임 3종 + 같은 네트워크 순위 경쟁 + 파일 하나로 배포**까지 AI(Claude Code)와 대화만으로 만든 과정입니다.

---

## 🎯 목표
💡 게임 아이디어 → 🎮 **두뇌 게임 3종(기억·사자성어·안전)** → 🏆 **같은 네트워크 순위 경쟁** → 📦 **파일 하나로 공유**

## 🧭 진행 과정 (대화 흐름 + 실제 코드)

### 1️⃣ 게임 기획 — 🪝 붙잡는 후크
> 💬 *"두뇌회전 + 자존심 + 경쟁심 + 광고까지 자극할 아이템이 뭐가 있을까"*
- 핵심은 **"한 끗 차이로 졌다"는 분함** → 재도전 유도
- 방향 확정: **기억력·패턴 / 하루 한 판 / 퍼센타일 순위**

### 2️⃣ 🧠 오늘의 기억 — 격자 기억 게임
> 💬 *"빛나는 칸 기억해서 똑같이 누르기, 순위 시스템 넣어서"*
```javascript
const MODES = {
  normal: { rounds:[ {size:3,tiles:3,view:1500}, ... {size:5,tiles:6,view:1000} ] }, // 일반 5R
  hard:   { scoreMul:5, rounds:[ {size:7,tiles:11,view:2600} ] }                     // 하드 7×7
};
```

### 3️⃣ 🖧 순위 서버 — 같은 네트워크 공유
> 💬 *"같은 네트워크 폴더 공유하는 사람끼리 기록 공유되게"*
```python
# 브라우저는 공유폴더에 직접 못 씀 → 작은 파이썬 서버가 scores.json에 모아줌
def upsert(records, name, score, age, mode, game):   # 사람·모드·게임별 최고점 유지
    ...
# GET /api/scores?game=X · POST /api/score → 같은 와이파이면 http://내IP:8000 로 접속
```

### 4️⃣ 🎛️ 게임 허브 + 게임별 순위 분리
> 💬 *"게임 선택 허브, 순위는 게임별로 나눠서 같은 서버 공유"*
```python
GAMES = ("memory", "idiom", "safety")
recs = [r for r in load_records() if (r.get("game") or "memory") == game]  # 게임별 필터
```

### 5️⃣ 📜 사자성어 스피드퀴즈 — 4지선다
> 💬 *"뜻을 보여주면 4지선다로 알맞은 사자성어 고르기, 10문제 문제당 5초"*
```javascript
function buildQuestions(){                       // 정답 + 오답 3개 랜덤 셔플
  return shuffle(IDIOMS).slice(0,TOTAL).map(ans=>({
    mean:ans.m, answer:ans.i,
    options:shuffle([ans, ...shuffle(others).slice(0,3)]).map(o=>o.i) }));
}
score += 100 + Math.round((remain/TIME_MS)*100);  // 정답 100 + 속도 보너스
```

### 6️⃣ 🏆 결과화면 통일 + 명예의 전당 + 내 최고 기록
> 💬 *"게임마다 배치가 달라서 어색해, 똑같이" · "하단에 명예의 전당 TOP5, 맨 아래 내 최고 기록"*
```javascript
// 서버 연결 → 실제 부서 순위 / 외부(파일·Pages) → 가짜 "전 세계 기준" 순위 (표는 항상 표시)
if (Net.available) renderDeptBoard(records, name);
else               renderGlobalBoard(score);
```

### 7️⃣ 🦺 안전 찍GO! — 현장 안전 상식 퀴즈
> 💬 *"안전 찍GO 게임 추가, 사자성어랑 같은 방식. 5문제. 좀 덜 전문적인 쉬운 걸로"*
```javascript
const QUIZ = [ {q:"공사장에서 머리 보호는?", a:"안전모", x:["야구모자","밀짚모자","비니"]}, ... ];
// 상단 3게임 전환 탭: 🧠 오늘의 기억 · 📜 사자성어 · 🦺 안전 찍GO!
```

### 8️⃣ 🚀 배포 — GitHub Pages + 파일로 열어도 순위 저장
> 💬 *"깃허브 배포도, 파일 공유해도 순위 되게, 서버 계속 켜두는 부담 줄이게"*
```javascript
// 서버가 띄우면 상대주소, 파일(file://)로 열리면 내 서버 주소로 자동 접속
const SERVER = 'http://192.168.20.64:8000';
const API = (location.protocol==='http:'||location.protocol==='https:') ? '' : SERVER;
```
```powershell
# 팀원 접속용 방화벽 — 이 게임(파이썬) + 8000 포트, 사내망에서만 허용
New-NetFirewallRule -DisplayName "BrainGame-8000" -Direction Inbound -Program $py `
  -Protocol TCP -LocalPort 8000 -Action Allow -Profile Private,Domain
```

### 9️⃣ 📦 한 파일 합본 + 🖼️ 아이콘
> 💬 *"공유는 파일 하나만, 어울리는 아이콘도"*
```javascript
// 세 게임을 각각 iframe에 격리 담아 충돌 0 → 두뇌게임.html 하나로
frame.srcdoc = u8(GAMES_B64[g]);   // base64로 안전하게 내장
```
```html
<!-- 3×3 빛나는 격자(메모리 게임 시그니처) 아이콘을 SVG로 내장 → 외부 파일 불필요 -->
<link rel="icon" href="data:image/svg+xml;base64, ...">
```

## 📦 최종 결과물
- 🎮 **`두뇌게임.html`** → 파일 하나로 3게임 + 게임 전환 (Z 공유폴더에 이거 하나만)
- 🖧 **`leaderboard_server.py` + `서버_켜기.bat`** → 같은 네트워크 순위 서버 (`scores.json`에 누적)
- 🌐 **GitHub Pages** (`ysg000.github.io/game`) → 아무나 게임(순위는 혼자 모드)
- 🖼️ **`두뇌게임.ico`** → 바로가기용 아이콘
- 📚 **`docs/` (PRD·PLAN·SKILL·CLAUDE.md)** → 요구사항·계획·런북·작업 규칙

## 💡 핵심 포인트
- 🔁 **"완벽한 지시"가 아니라 "대화"로** → 만들고 → 보고 → 고치고 → 다시 확인
- 🖧 순위 공유의 본질 = **파일 공유가 아니라 서버(HTTP)** → 파일은 Z에, 저장은 내 PC(C)로
- 🚩 애매한 건(공유 방식, 서버 위치, 권한) **짐작하지 않고 확실히 질문·설명**
- 🐞 함정도 같이 해결: bat 인코딩(CP949)·줄바꿈(CRLF), file:// 홈버튼 C드라이브 튐, 아이콘 캐시, 방화벽
- 🧱 게임 1종 → 허브·게임별 순위 → 3종 → 배포 → **한 파일 합본**까지 한 단계씩 확장

---
*🤖 AI(Claude Code)와의 협업 기록 · 사내 두뇌 게임 순위판 만들기*
