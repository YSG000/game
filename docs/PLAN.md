# PLAN — 구현 계획 & 로드맵

> "어떻게, 언제" 만드는지의 실행 문서. 다음에 할 일을 여기서 고른다.
> 상태 표기: [x] 완료 / [~] 진행중 / [ ] 예정

## 현재 상태 (2026-07-01 기준)
게임·서버·로컬 네트워크 순위·GitHub Pages 배포까지 완성. 파일은 `test1/` 폴더에서 관리.

## 파일 구성
| 파일 | 역할 |
|---|---|
| `hub.html` | 게임 선택 허브 (서버 `/` 진입점) |
| `memory_daily_prototype.html` | 게임① 오늘의 기억 (최종본, 기준 파일) |
| `idiom.html` | 게임② 사자성어 도전 (4지선다) |
| `index.html` | GitHub Pages 배포용 복사본(메모리 게임) |
| `leaderboard_server.py` | 순위 서버 (게임별 순위 분리) |
| `서버_켜기.bat` | 더블클릭 실행기 |
| `scores.json` | 순위 기록 (git 제외) |

## 완료된 일
- [x] 격자 기억 게임 (일반/하드 모드)
- [x] 파이썬 순위 서버 (`/api/scores` GET, `/api/score` POST)
- [x] cp949 콘솔 크래시 버그 수정 (`_safe_print`)
- [x] 최고점만 유지 + 플레이 횟수 누적 (`upsert`)
- [x] CORS 허용 (배포용 파일에서 서버 접속 대비)
- [x] GitHub 업로드 (`YSG000/game`) + Pages 배포
- [x] VSVS → test1 하위 폴더로 이동 (완료됨)
- [x] **게임 선택 허브(`hub.html`)** — 서버 `/`가 허브로 진입
- [x] **게임② 사자성어 도전(`idiom.html`)** — 뜻→사자성어 4지선다, 10문제·문제당 5초
- [x] **서버 게임별 순위 분리** — `game` 필드 추가, 기존 기록은 자동 `memory`(하위호환)
  - 검증: 라우팅(/·/memory·/idiom), idiom↔memory 순위 분리, UTF-8 저장 모두 통과

## 다음 후보 (우선순위 순)
- [ ] **서버_켜기.bat에 실제 IP 자동 표시** — 공유 주소를 창에 바로 출력
- [ ] **본인 순위에 `(나)` 강조 표시**
- [ ] **조용히 켜기 3종 세트** — `서버_조용히_켜기.vbs` / `서버_끄기.bat` / HTML 캐시 무효화
- [ ] **윈도우 방화벽 8000 포트 안내/설정**
- [ ] **배포용 HTML** — 서버 IP를 박아넣어 파일 배포로도 저장되게

## 큰 갈림길 (결정 필요)
- [ ] **클라우드 전환** (PC 안 켜도 순위 유지)
  - 후보: Supabase(추천, 표 기반) / Firebase / Render(파이썬 호스팅)
  - 필요 작업: 무료 계정 → HTML 점수 저장 부분 수정 → 재배포
  - 트레이드오프: 관리 편함 ↔ 기록이 내 폴더가 아닌 클라우드에 저장

## 알려진 함정
- git 방금 설치 시 기존 터미널이 PATH 미인식 → 매 명령마다 PATH 새로고침 or VS Code 재시작
- 파일 직접 열기(file://)는 서버 미연결 → 반드시 `http://…:8000`로 접속해야 저장됨
